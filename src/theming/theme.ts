import { createTheme } from "@mui/material"

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "2rem",
    },
    h2: {
      fontSize: "1.8rem",
    },
    h4: {
      fontSize: "1.2rem",
    },
  },
})

export default theme